[build]
  command = "rm -rf .quartz/plugins && npx quartz plugin install --from-config --clean && npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10.9.2"

[build.processing.html]
  pretty_urls = true
